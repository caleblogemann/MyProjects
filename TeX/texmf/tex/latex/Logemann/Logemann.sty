\NeedsTeXFormat{LaTeX2e}[1994/06/01]
\ProvidesPackage{Logemann}[2014/09/08 Logemann Package]

% set up language and font encoding
\RequirePackage[english]{babel}
\RequirePackage[T1]{fontenc}
\RequirePackage{lmodern}

% needed for \DeclarePairedDelimiter
\RequirePackage{mathtools}
% common packages used in almost all documents
\RequirePackage{amsthm}
\RequirePackage{amssymb}
\RequirePackage{amsmath}
\RequirePackage{booktabs}

% absolute value function
\DeclarePairedDelimiter\abs{\lvert}{\rvert}
% Swap the definition of \abs* , so that \abs
% resizes the size of the brackets, and the
% starred version does not.
\let\oldabs\abs
\def\abs{\@ifstar{\oldabs}{\oldabs*}}

% Norm - length of a vector
\DeclarePairedDelimiter\norm{\lVert}{\rVert}
\let\oldnorm\norm
\def\norm{\@ifstar{\oldnorm}{\oldnorm*}}

% Floor function
\DeclarePairedDelimiter{\floor}{\lfloor}{\rfloor}
\let\oldfloor\floor
\def\floor{\@ifstar{\oldfloor}{\oldfloor*}}

% Ceiling function
\DeclarePairedDelimiter{\ceil}{\lceil}{\rceil}
\let\oldceil\ceil
\def\ceil{\@ifstar{\oldceil}{\oldceil*}}

% Parenthesis
\DeclarePairedDelimiter{\paren}{(}{)}
\let\oldparen\paren
\def\paren{\@ifstar{\oldparen}{\oldparen*}}
\def\p{\@ifstar{\oldparen}{\oldparen*}}

% Curly Brackets
\DeclarePairedDelimiter{\set}{\{}{\}}
\let\oldset\set
\def\set{\@ifstar{\oldset}{\oldset*}}

% Square Brackets
\DeclarePairedDelimiter{\br}{[}{]}
\let\oldbr\br
\def\br{\@ifstar{\oldbr}{\oldbr*}}

% use \cos default to include parenthesis
% \cos* will not have parenthesis
% applies to all future commands
\let\oldcos\cos
\newcommand{\cosa}[1]{\oldcos\paren{#1}}
\def\cos{\@ifstar{\oldcos}{\cosa}}

\let\oldsin\sin
\newcommand{\sina}[1]{\oldsin\paren{#1}}
\def\sin{\@ifstar{\oldsin}{\sina}}

\let\oldarcsin\arcsin
\newcommand{\arcsina}[1]{\oldarcsin\paren{#1}}
\def\arcsin{\@ifstar{\oldarcsin}{\arcsina}}

\let\oldarccos\arccos
\newcommand{\arccosa}[1]{\oldarccos\paren{#1}}
\def\arccos{\@ifstar{\oldarccos}{\arccosa}}

\let\oldarctan\arctan
\newcommand{\arctana}[1]{\oldarctan\paren{#1}}
\def\arctan{\@ifstar{\oldarctan}{\arctana}}

\let\oldtan\tan
\newcommand{\tana}[1]{\oldtan\paren{#1}}
\def\tan{\@ifstar{\oldtan}{\tana}}

\let\oldcsc\csc
\newcommand{\csca}[1]{\oldcsc\paren{#1}}
\def\csc{\@ifstar{\oldcsc}{\csca}}

\let\oldsec\sec
\newcommand{\seca}[1]{\oldsec\paren{#1}}
\def\sec{\@ifstar{\oldsec}{\seca}}

\let\oldcot\cot
\newcommand{\cota}[1]{\oldcot\paren{#1}}
\def\cot{\@ifstar{\oldcot}{\cota}}

\let\oldln\ln
\newcommand{\lna}[1]{\oldln\paren{#1}}
\def\ln{\@ifstar{\oldln}{\lna}}

\let\oldlog\log
\newcommand{\loga}[1]{\oldlog\paren{#1}}
\def\log{\@ifstar{\oldlog}{\loga}}

\newcommand{\logbaseparen}[2]{\oldlog_{#1}\paren{#2}}
\newcommand{\logbase}[1]{\oldlog_{#1}}
\def\logb{\@ifstar{\logbase}{\logbaseparen}}

\let\oldmin\min
\newcommand{\mina}[1]{\oldmin\set{#1}}
\def\min{\@ifstar{\oldmin}{\mina}}

\let\oldmax\max
\newcommand{\maxa}[1]{\oldmax\set{#1}}
\def\max{\@ifstar{\oldmax}{\maxa}}
